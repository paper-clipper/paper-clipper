import styled from 'styled-components'
import {
    colorPrimary,
} from '@paper/layout/mixins'

export default styled.span`
    font-size: 0.625rem;
    color: ${colorPrimary};
    margin: 0 0.625rem;
`
