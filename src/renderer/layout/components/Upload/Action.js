import styled from 'styled-components'
import {
    colorNeutral,
} from '@paper/layout/mixins'

export default styled.i`
    color: ${colorNeutral};
    font-size: 0.875rem;
    cursor: pointer;
`
